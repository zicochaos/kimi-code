---
"@moonshot-ai/kimi-code": patch
---

Add optional `persist_default_model = false` so model switches stay session-only and do not rewrite a managed `config.toml`. Set `persist_default_model = false` in `config.toml`.
