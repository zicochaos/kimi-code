---
"@moonshot-ai/kimi-code": minor
---

Add native /provider setup for OpenAI-compatible providers. Use /provider to enter a provider base URL and API key, fetch models, and select them through /model.
