---
"@moonshot-ai/kimi-code": patch
---

Add optional `agents_md_expand_includes = true` so `@path` lines in AGENTS.md are inlined at session start. Set `agents_md_expand_includes = true` in `config.toml`.
