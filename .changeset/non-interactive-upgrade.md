---
"@moonshot-ai/kimi-code": minor
---

Add `--yes` flag to `kimi upgrade` for non-interactive installs. The short
`-y` form is intentionally omitted because it collides with the root `-y, --yolo`
auto-approve flag.
