/** Shared host identity for klient examples (bootstrap requires one). */
export const EXAMPLE_CLIENT_IDENTITY = {
  productName: 'kimi-code-example',
  version: '0.0.0-example',
  platform: 'example',
} as const;
