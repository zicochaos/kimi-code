/**
 * `skill` domain barrel — re-exports the skill contract (`skill`) and its
 * scoped services (`skillService`). Importing this barrel registers the
 * `ISkillRegistry` and `ISkillService` bindings into the scope registry.
 */

export * from './skill';
export * from './skillService';
