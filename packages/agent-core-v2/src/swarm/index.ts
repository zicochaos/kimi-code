/**
 * `swarm` domain barrel — re-exports the swarm contract (`swarm`) and its
 * scoped service (`swarmService`). Importing this barrel registers the
 * `ISwarmService` binding into the scope registry.
 */

export * from './swarm';
export * from './swarmService';
