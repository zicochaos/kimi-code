/**
 * `records` domain barrel — re-exports the records contract (`records`) and
 * its scoped services (`recordsService`). Importing this barrel registers the
 * records bindings into the scope registry.
 */

export * from './records';
export * from './recordsService';
