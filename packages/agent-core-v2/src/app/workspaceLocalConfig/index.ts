/**
 * `workspaceLocalConfig` domain barrel — re-exports the project-local config
 * contract (`workspaceLocalConfig`). The node-fs backend registers the
 * `IWorkspaceLocalConfigService` binding.
 */

export * from './workspaceLocalConfig';
