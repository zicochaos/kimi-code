/**
 * `context` domain barrel — re-exports the context contract (`context`) and
 * its scoped service (`contextService`). Importing this barrel registers the
 * `IContextService` binding into the scope registry.
 */

export * from './context';
export * from './contextService';
