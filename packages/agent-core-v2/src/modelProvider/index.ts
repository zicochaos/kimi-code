/**
 * `modelProvider` domain barrel — re-exports the model provider contract,
 * model config-section schema, and host-seeded implementations.
 */

export * from './modelProvider';
export * from './configSection';
export * from './modelProviderService';
