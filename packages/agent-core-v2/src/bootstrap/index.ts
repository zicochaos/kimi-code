/**
 * `bootstrap` domain barrel — re-exports the bootstrap contract (`bootstrap`)
 * and its scoped service (`bootstrapService`). Importing this barrel registers
 * the `IBootstrapService` binding into the scope registry.
 */

export * from './bootstrap';
export * from './bootstrapService';
