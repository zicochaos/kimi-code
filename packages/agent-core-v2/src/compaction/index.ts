/**
 * `compaction` domain barrel — re-exports the compaction contract
 * (`compaction`) and its scoped service (`compactionService`). Importing this
 * barrel registers the `ICompactionService` binding into the scope registry.
 */

export * from './compaction';
export * from './compactionService';
