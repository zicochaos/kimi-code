# Config Section Manifest
#
# Generated by scripts/gen-config-manifest.mts — do not edit by hand.
# Regenerate with: pnpm --filter @moonshot-ai/agent-core-v2 gen:config-manifest
#
# One [table] per registered config section, in the on-disk config.toml shape
# (snake_case keys). Un-commented assignments are registered defaults;
# commented "# field: type" lines describe the remaining schema fields.
# Values resolve as: default -> config.toml -> env overlay -> memory.

# Index (28 sections · 2 overlay(s))
#   agentsMdExpandIncludes   src/agent/profile/configSection.ts
#   background               src/agent/task/configSection.ts
#   builtinProductSkills     src/app/skillCatalog/configSection.ts
#   cron                     src/app/cron/configSection.ts
#   defaultPermissionMode    src/agent/permissionMode/configSection.ts
#   defaultPlanMode          src/features/plan/configSection.ts
#   disabledSkills           src/app/skillCatalog/configSection.ts
#   experimental             src/app/flag/flag.ts
#   extraAgentDirs           src/workspace/workspaceAgentProfileLoader/configSection.ts
#   extraSkillDirs           src/app/skillCatalog/configSection.ts
#   hooks                    src/agent/externalHooks/configSection.ts
#   identity                 src/app/agentIdentity/configSection.ts
#   image                    src/agent/media/configSection.ts
#   loopControl              src/agent/loop/configSection.ts
#   mcp                      src/app/mcpConfig/configSection.ts
#   mergeAllAvailableSkills  src/app/skillCatalog/configSection.ts
#   modelCatalog             src/app/kosongConfig/configSection.ts
#   models                   src/app/kosongConfig/configSection.ts
#   permission               src/agent/permissionRules/configSection.ts
#   persistDefaultModel      src/app/kosongConfig/configSection.ts
#   providers                src/app/kosongConfig/configSection.ts
#   secondaryModel           src/session/subagent/configSection.ts
#   services                 src/app/auth/configSection.ts
#   subagent                 src/session/subagent/configSection.ts
#   task                     src/agent/task/configSection.ts
#   thinking                 src/app/kosongConfig/configSection.ts
#   tokenCounting            src/agent/tokenCounting/configSection.ts
#   tools                    src/agent/toolPolicy/configSection.ts
#   (overlay) servicesCredentialEnvOverlay  src/app/auth/configSection.ts
#   (overlay) kimiModelEnvOverlay  src/app/kosongConfig/envOverlay.ts

# ##########################################################################
# agentsMdExpandIncludes (config.toml: agents_md_expand_includes)
#   owner: src/agent/profile/configSection.ts
#   scope: core
# ##########################################################################

agents_md_expand_includes = false

# ##########################################################################
# background
#   owner: src/agent/task/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     keep_alive_on_exit <- KIMI_CODE_BACKGROUND_KEEP_ALIVE_ON_EXIT (custom parse)
#     max_running_tasks <- KIMI_CODE_BACKGROUND_MAX_RUNNING_TASKS (custom parse)
# ##########################################################################

[background]
# max_running_tasks: integer
# keep_alive_on_exit: boolean
# bash_auto_background_on_timeout: boolean
# bash_task_timeout_s: integer
# kill_grace_period_ms: integer
# print_wait_ceiling_s: integer
# print_background_mode: "exit" | "drain" | "steer"
# print_max_turns: integer

# ##########################################################################
# builtinProductSkills (config.toml: builtin_product_skills)
#   owner: src/app/skillCatalog/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#      <- KIMI_CODE_BUILTIN_PRODUCT_SKILLS (custom parse)
# ##########################################################################

builtin_product_skills = true

# ##########################################################################
# cron
#   owner: src/app/cron/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     debug <- KIMI_CRON_DEBUG (custom parse)
#     no_jitter <- KIMI_CRON_NO_JITTER (custom parse)
#     no_stale <- KIMI_CRON_NO_STALE (custom parse)
#     disabled <- KIMI_DISABLE_CRON (custom parse)
#     manual_tick <- KIMI_CRON_MANUAL_TICK (custom parse)
#     clock <- KIMI_CRON_CLOCK
#     poll_interval_ms <- KIMI_CRON_POLL_INTERVAL_MS (custom parse)
# ##########################################################################

[cron]
# (schema uses transforms; fields below come from the registered default)
debug = false
no_jitter = false
no_stale = false
disabled = false
manual_tick = false

# ##########################################################################
# defaultPermissionMode (config.toml: default_permission_mode)
#   owner: src/agent/permissionMode/configSection.ts
#   scope: core
# ##########################################################################

# default_permission_mode: "manual" | "auto" | "yolo"

# ##########################################################################
# defaultPlanMode (config.toml: default_plan_mode)
#   owner: src/features/plan/configSection.ts
#   scope: core
# ##########################################################################

default_plan_mode = false

# ##########################################################################
# disabledSkills (config.toml: disabled_skills)
#   owner: src/app/skillCatalog/configSection.ts
#   scope: core
# ##########################################################################

disabled_skills = []

# ##########################################################################
# experimental
#   owner: src/app/flag/flag.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[experimental]
# <name>: boolean

# ##########################################################################
# extraAgentDirs (config.toml: extra_agent_dirs)
#   owner: src/workspace/workspaceAgentProfileLoader/configSection.ts
#   scope: core
# ##########################################################################

extra_agent_dirs = []

# ##########################################################################
# extraSkillDirs (config.toml: extra_skill_dirs)
#   owner: src/app/skillCatalog/configSection.ts
#   scope: core
# ##########################################################################

extra_skill_dirs = []

# ##########################################################################
# hooks
#   owner: src/agent/externalHooks/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

# one [[hooks]] table per entry:
# [[hooks]]
  # event: "PreToolUse" | "PostToolUse" | "PostToolUseFailure" | "PermissionRequest" | "PermissionResult" | "UserPromptSubmit" | "UserPromptQueued" | "TurnStarted" | "Stop" | "StopFailure" | "Interrupt" | "SessionStart" | "SessionEnd" | "SessionHeartbeat" | "SubagentStart" | "SubagentStop" | "TaskStarted" | "PreCompact" | "PostCompact" | "Notification"
  # matcher: string
  # command: string
  # timeout: integer

# ##########################################################################
# identity
#   owner: src/app/agentIdentity/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     name <- KIMI_CODE_IDENTITY_NAME (custom parse)
#     slug <- KIMI_CODE_IDENTITY_SLUG (custom parse)
# ##########################################################################

[identity]
# name: string
# slug: string

# ##########################################################################
# image
#   owner: src/agent/media/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     max_edge_px <- KIMI_IMAGE_MAX_EDGE_PX (custom parse)
#     read_byte_budget <- KIMI_IMAGE_READ_BYTE_BUDGET (custom parse)
# ##########################################################################

[image]
# max_edge_px: integer
# read_byte_budget: integer

# ##########################################################################
# loopControl (config.toml: loop_control)
#   owner: src/agent/loop/configSection.ts
#   scope: core
#   hooks: custom toToml · stripEnv
#   deprecations (old key is ignored + warns; rename manually):
#     max_retries_per_step -> max_attempts_per_step
#     max_steps_per_run -> max_steps_per_turn
#   env:
#     max_steps_per_turn <- KIMI_LOOP_MAX_STEPS_PER_TURN (custom parse)
#     max_attempts_per_step <- KIMI_LOOP_MAX_ATTEMPTS_PER_STEP (custom parse; deprecated fallback KIMI_LOOP_MAX_RETRIES_PER_STEP)
# ##########################################################################

[loop_control]
# max_steps_per_turn: integer
# max_attempts_per_step: integer
# max_ralph_iterations: integer
# reserved_context_size: integer
# compaction_trigger_ratio: number

# ##########################################################################
# mcp
#   owner: src/app/mcpConfig/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     startup_timeout_ms <- KIMI_MCP_STARTUP_TIMEOUT_MS (custom parse)
#     tool_timeout_ms <- KIMI_MCP_TOOL_TIMEOUT_MS (custom parse)
# ##########################################################################

[mcp]
# startup_timeout_ms: integer
# tool_timeout_ms: integer

# ##########################################################################
# mergeAllAvailableSkills (config.toml: merge_all_available_skills)
#   owner: src/app/skillCatalog/configSection.ts
#   scope: core
# ##########################################################################

merge_all_available_skills = true

# ##########################################################################
# modelCatalog (config.toml: model_catalog)
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
# ##########################################################################

[model_catalog]
# refresh_interval_ms: integer
# refresh_on_start: boolean

# ##########################################################################
# models
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[models]

# one [models."<name>"] table per entry:
# [models."<name>"]
  # provider_id: string
  # base_url: string
  # api_key: string
  # oauth: object
    # storage: "file" | "keyring"
    # key: string
    # oauth_host: string
  # protocol: "anthropic" | "openai" | "openai_responses" | "google-genai"
  # name: string
  # aliases: string[]
  # provider: string
  # model: string
  # max_context_size: integer
  # max_input_size: integer
  # max_output_size: integer
  # capabilities: string[]
  # display_name: string
  # reasoning_key: string
  # adaptive_thinking: boolean
  # beta_api: boolean
  # support_efforts: string[]
  # default_effort: string
  # off_effort: string
  # overrides: object
    # max_context_size: integer
    # max_input_size: integer
    # max_output_size: integer
    # capabilities: string[]
    # display_name: string
    # reasoning_key: string
    # adaptive_thinking: boolean
    # support_efforts: string[]
    # default_effort: string
    # off_effort: string

# ##########################################################################
# permission
#   owner: src/agent/permissionRules/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[permission]
# rules: object[] — one entry per item:
  # decision: "allow" | "deny" | "ask"
  # scope: "turn-override" | "session-runtime" | "project" | "user" (default: "user")
  # pattern: string
  # reason: string

# ##########################################################################
# persistDefaultModel (config.toml: persist_default_model)
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
# ##########################################################################

persist_default_model = true

# ##########################################################################
# providers
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml · stripEnv
#   env:
#     __kimi_env__.api_key <- KIMI_MODEL_API_KEY
#     __kimi_env__.type <- KIMI_MODEL_PROVIDER_TYPE
#     __kimi_env__.base_url <- KIMI_MODEL_BASE_URL
# ##########################################################################

[providers]

# one [providers."<name>"] table per entry:
# [providers."<name>"]
  # model_source: "static" | "discover" | "oauth-catalog"
  # base_url: string
  # custom_headers: record<string, string>
  # default_model: string
  # type: string
  # api_key: string
  # oauth: object
    # storage: "file" | "keyring"
    # key: string
    # oauth_host: string
  # env: record<string, string>
  # source: record<string, any>

# ##########################################################################
# secondaryModel (config.toml: secondary_model)
#   owner: src/session/subagent/configSection.ts
#   scope: core
# ##########################################################################

[secondary_model]
# default_model: string
# models: record<string, string>
# force: boolean
# model: string
# max_context_size: integer
# max_input_size: integer
# max_output_size: integer
# capabilities: string[]
# display_name: string
# reasoning_key: string
# adaptive_thinking: boolean
# support_efforts: string[]
# default_effort: string
# off_effort: string

# ##########################################################################
# services
#   owner: src/app/auth/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml · stripEnv
#   env:
#     moonshot_search.base_url <- KIMI_WEB_SEARCH_BASE_URL (custom parse)
#     moonshot_search.api_key <- KIMI_WEB_SEARCH_API_KEY (custom parse)
#     moonshot_fetch.base_url <- KIMI_WEB_FETCH_BASE_URL (custom parse)
#     moonshot_fetch.api_key <- KIMI_WEB_FETCH_API_KEY (custom parse)
# ##########################################################################

[services]

# [services.moonshot_search]
  # base_url: string
  # api_key: string
  # oauth: object
    # storage: "file" | "keyring"
    # key: string
    # oauth_host: string
  # custom_headers: record<string, string>

# [services.moonshot_fetch]
  # base_url: string
  # api_key: string
  # oauth: object
    # storage: "file" | "keyring"
    # key: string
    # oauth_host: string
  # custom_headers: record<string, string>

# ##########################################################################
# subagent
#   owner: src/session/subagent/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     timeout_ms <- KIMI_SUBAGENT_TIMEOUT_MS (custom parse)
# ##########################################################################

[subagent]
timeout_ms = 7200000

# ##########################################################################
# task
#   owner: src/agent/task/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     keep_alive_on_exit <- KIMI_CODE_BACKGROUND_KEEP_ALIVE_ON_EXIT (custom parse)
#     max_running_tasks <- KIMI_CODE_BACKGROUND_MAX_RUNNING_TASKS (custom parse)
# ##########################################################################

[task]
# max_running_tasks: integer
# keep_alive_on_exit: boolean
# bash_auto_background_on_timeout: boolean
# bash_task_timeout_s: integer
# kill_grace_period_ms: integer
# print_wait_ceiling_s: integer
# print_background_mode: "exit" | "drain" | "steer"
# print_max_turns: integer

# ##########################################################################
# thinking
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     forced_effort <- KIMI_MODEL_THINKING_EFFORT
# ##########################################################################

[thinking]
# enabled: boolean
# effort: string
# forced_effort: string
# keep: string

# ##########################################################################
# tokenCounting (config.toml: token_counting)
#   owner: src/agent/tokenCounting/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     strategy <- KIMI_TOKEN_COUNTING_STRATEGY (custom parse)
# ##########################################################################

[token_counting]
strategy = "measured+estimated"

# ##########################################################################
# tools
#   owner: src/agent/toolPolicy/configSection.ts
#   scope: core
# ##########################################################################

[tools]
# enabled: string[]
# disabled: string[]
